// !$*UTF8*$!
{
	archiveVersion = 1;
	classes = {
	};
	objectVersion = 60;
	objects = {
/* This is a placeholder. Open this directory in Xcode and create a new iOS App project named DoerApp with:
   - Interface: SwiftUI
   - Language: Swift
   - Minimum Deployment: iOS 17.0

   Then add the Swift Package dependency:
   - URL: https://github.com/supabase/supabase-swift
   - Version: 2.0.0 or later

   Copy all .swift files from this directory structure into the Xcode project.
*/
	};
}
